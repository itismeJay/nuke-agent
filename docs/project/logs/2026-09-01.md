# 2026-09-01

## Version control initialized and pushed to GitHub

- **Phase:** Phase 0 — Engineering Foundation
- **What changed:** Committed the existing Next.js project as the first commit (`a46b5bf`, 69 tracked files, `.gitignore` respected so `node_modules/` and `.next/` are excluded). Renamed the default branch `master` → `main`. Added remote `origin` = `https://github.com/itismeJay/nuke-agent.git` and pushed `main` with upstream tracking. Prepended a `# nuke-agent` title to `README.md` (which already held create-next-app content) instead of appending.
- **Database/migrations:** None.
- **Security/authorization:** `.gitignore` confirmed to exclude `.env*`, `/node_modules`, `/.next`, `*.pem`. No secrets committed.
- **Tests/verification:** `git push` succeeded (`new branch main -> main`); working tree clean; branch tracks `origin/main`.
- **Decisions:** None new.
- **Known issues:** No GitHub CI, secret scanning, or dependency security baseline yet (Phase 0 checklist still open). `/remember` workflow doc references `docs/project/...` but the IDE-opened path was `context/docs/project/...`; actual docs live at `docs/project/`.
- **Next:** Continue Phase 0 (CI for install/lint/typecheck/test/build, secret scanning) or resume Phase 1 database/auth work.

---

## Phase 1 — Database & Authentication implemented (`/phase 1`)

- **Phase:** Phase 1 — Database & Authentication
- **What changed:**
  - **Supabase clients / lib:** `lib/supabase/{client,server,middleware,admin}.ts`,
    `lib/supabase/database.types.ts` (from MCP-generated types), `lib/env.ts`
    (validated public/secret env split), `lib/utils.ts` (shadcn `cn`).
  - **Auth:** `lib/auth/{actions,account,user}.ts` — server actions (sign in / up /
    out, forgot + reset password, Google OAuth start), `ensureAccountInitialized`
    idempotent fallback, `requireUser`/`getCurrentUser`. `app/auth/callback/route.ts`
    exchanges the OAuth/email-link code and seeds base rows.
  - **Route protection:** `middleware.ts` → `lib/supabase/middleware.ts`
    (`updateSession`) refreshes the session and guards `/dashboard /profile /jobs
    /applications /resumes /settings`; redirects signed-out → `/sign-in?redirectTo=`,
    signed-in → away from auth pages.
  - **UI:** `shadcn init` was run (base-ui variant, `components.json`, base-nova
    style) with `button/card/input/label/dropdown-menu/sheet/separator/avatar/
    badge/skeleton/sonner` primitives. Nook design tokens rewritten into
    `app/globals.css` (light + `.dark`, `next-themes` system default). Marketing
    landing (`app/page.tsx` + `app/(marketing)`), split-layout auth screens
    (`app/(auth)`), authenticated shell with sidebar + mobile Sheet drawer + user
    menu (`app/(app)` — dashboard is real, other pages are phase-labelled
    placeholders). `/ui-review` pass done on the auth pages afterwards.
  - **Config:** `.env.example` documented, `.env.local` populated with the
    `nook-agent` URL + publishable key, `.gitignore` keeps `.env.example`, README
    rewritten with real local setup.
  - Added deps: `@supabase/ssr`, `@supabase/supabase-js`, `next-themes`, plus the
    shadcn-pulled `@base-ui/react`, `class-variance-authority`, `clsx`,
    `tailwind-merge`, `lucide-react`, `tw-animate-css`, `shadcn`.
- **Database/migrations:**
  - Vendored the 3 pre-existing migrations into `supabase/migrations/` verbatim
    (`init_multitenant_schema`, `harden_functions`, `per_user_job_status`) + wrote
    `supabase/README.md`.
  - Applied **1 new migration** `20260901120000_idempotent_account_initialization`
    — `handle_new_user()` now uses `on conflict (user_id) do nothing` for both
    `profile` and `agent_settings`.
  - Regenerated `lib/supabase/database.types.ts`.
  - Dashboard config (done by owner): email "Confirm email" turned **off**
    (`mailer_autoconfirm: true`); Google provider **enabled** with a Google Cloud
    Web OAuth client (redirect URI `https://lemtlbepgrkltkmjbmqy.supabase.co/auth/v1/callback`);
    Site URL `http://localhost:3000` + Redirect URL `http://localhost:3000/**`.
- **Security/authorization:**
  - RLS verified live, not just assumed: `pg_policies` audit shows every user-owned
    table has one `"own rows"` policy `for all to authenticated using/with check
    (user_id = (select auth.uid()))`; `job` is `select`-only for `authenticated`.
  - Two-user isolation test: User B could not SELECT (0 rows), UPDATE (0 affected),
    or DELETE (0 affected) User A's `experience` row; A's row stayed intact; `anon`
    role sees nothing on every table; WITH CHECK blocks inserting rows owned by
    another user. Test users deleted afterwards (cascade cleans child rows).
  - Real-JWT check via PostgREST: a signed-in user reads only their own
    `profile`/`agent_settings` and an empty `experience` set.
  - Admin/service client (`lib/supabase/admin.ts`) is `import "server-only"` and
    throws unless `SUPABASE_SERVICE_ROLE_KEY` is set — the key is not set anywhere.
  - `friendlyAuthError` maps GoTrue errors to non-enumerating copy; `redirectTo`
    is sanitised to same-origin relative paths only.
- **Tests/verification:**
  - `npm run lint` clean, `npm run build` green (TypeScript passes, 15 routes).
  - Browser: signed in with an email/password test user → landed on `/dashboard`
    (app shell + seeded rows), visited `/settings`, signed out → landing page
    (signed-out nav), then `/dashboard` → `307 /sign-in?redirectTo=%2Fdashboard`.
  - Email/password signup returns an active session immediately (autoconfirm on).
  - Google: `/auth/v1/authorize?provider=google` 302s to Google's account chooser
    with the right `client_id` + `redirect_uri`, no `redirect_uri_mismatch`. Full
    credential round-trip not exercised (needs the owner's Google account).
  - No test runner exists, so no automated tests were added.
- **Decisions:** D-011 (shadcn base-ui + next-themes as the UI layer), D-012
  (keep `middleware.ts`, not Next 16 `proxy.ts`), D-013 (email confirmation off in
  dev). See DECISIONS.md.
- **Known issues:** TD-001 (`middleware` deprecation), TD-002 (no test runner / CI —
  blocks a real `/verify`), TD-003 (`database.types.ts` regenerated by hand),
  TD-004 (`next build`/`next dev` auto-appends a block to `AGENTS.md`). See
  TECH_DEBT.md. The browser screenshot tool renders desktop width only, so the
  mobile nav drawer is built-but-not-screenshotted.
- **Next:** Owner runs a real Google sign-in through to `/dashboard` to fully
  close Phase 1's "Done when", then Phase 2 — Career Profile.

### Follow-up fix (same day)

- `components/ui/button.tsx`: `<Button render={<Link/>}>` triggered a Base UI
  console error (`nativeButton` true but rendered element is not a `<button>`).
  Fixed by defaulting `nativeButton` to `false` whenever a `render` prop is
  passed. Verified console-clean on landing / sign-in / sign-up / dashboard;
  lint + build green.

### shadcn-first UI rule + refactor (same day)

- **Rule added** in four places: `CLAUDE.md` (`## Frontend / UI Rules`),
  `docs/design/UI_SYSTEM.md` (`# Component Library`), `.agent/workflows/ui.md`
  (`## Component Library Requirement`), `AGENTS.md` (core-behavior bullet) —
  "use shadcn/ui as the default component library; prefer existing / `npx shadcn
  add` over custom primitives."
- **Refactor of the shipped UI to follow it:**
  - Added primitives: `alert`, `field`, `empty`, `spinner`, `tooltip`, `sidebar`
    (+ `hooks/use-mobile.ts`, rewritten to `useSyncExternalStore` so it passes
    `react-hooks/set-state-in-effect`).
  - App shell rebuilt on the shadcn **`sidebar`** system: `SidebarProvider`
    (reads `sidebar_state` cookie) + `Sidebar` + `SidebarInset` in
    `app/(app)/layout.tsx`; `AppSidebar` uses `SidebarHeader/Content/Group/Menu/
    Footer`; nav is `SidebarMenuButton isActive tooltip render={<Link/>}`; user
    menu is a `DropdownMenu` on a `SidebarMenuButton`. Deleted the hand-rolled
    `mobile-header.tsx`, `nav-list.tsx`; mobile drawer is now the sidebar's
    built-in Sheet via `SidebarTrigger`.
  - Auth forms rebuilt on **`Field`** (`FieldGroup/Field/FieldLabel/FieldError/
    FieldDescription`); the "or" divider is `FieldSeparator` (deleted custom
    `or-divider.tsx`); `FormMessage` now wraps shadcn **`Alert`**; loading
    spinners use shadcn **`Spinner`**.
  - Placeholder bodies (`ComingSoon`, dashboard) use shadcn **`Empty`**; landing
    workflow preview wrapped in **`Card`**.
  - `next.config.ts`: `devIndicators.position = "bottom-right"` so the Next dev
    badge stops overlapping the sidebar footer.
- **Bug fixed during refactor:** `user-menu.tsx` had `DropdownMenuLabel` outside
  a group → Base UI `MenuGroupContext is missing` crash when the menu opened
  (latent — the menu was never opened in earlier testing). Moved the label
  inside `DropdownMenuRadioGroup`.
- **Verified:** `lint` + `build` green; browser console clean on landing /
  sign-in / sign-up / forgot-password / dashboard; user dropdown opens (theme
  switch + sign out); sign-out → landing. `rbjay2005@gmail.com` is a real
  Google-auth account now in the DB (created ~10:25 UTC) — left intact; it was
  signed out in the browser during testing.
