# inngest@4 declares a wide set of framework integrations as optional peer
# dependencies (SvelteKit, etc.) whose own transitive peers (vite ^8) conflict
# with the vite that vitest pulls in. We only use inngest/next. legacy peer
# resolution avoids the phantom conflict; `npm ci` in CI reads this file too.
legacy-peer-deps=true
